/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'mir-s3-cdn-cf.behance.net', 'i.pinimg.com'],
    },
};

module.exports = nextConfig;